/**
 * 🦀 ЗАДАЧА 3 НА RUST: Обмен значений
 */

fn main() {
    println!("=== ЗАДАЧА 3: Обмен значений (Rust) ===");
    
    let mut a = 10;
    let mut b = 20;
    println!("До обмена: a = {}, b = {}", a, b);
    
    // Способ 1: Через временную переменную (как в C++)
    {
        let ref_a = &mut a;
        let ref_b = &mut b;
        
        let temp = *ref_a;
        *ref_a = *ref_b;
        *ref_b = temp;
    } // ссылки "умирают" здесь
    
    println!("После обмена (способ 1): a = {}, b = {}", a, b);
    
    // Способ 2: Rust-овский элегантный способ!
    std::mem::swap(&mut a, &mut b);  // Встроенная функция обмена
    
    println!("После второго обмена (способ 2): a = {}, b = {}", a, b);
    
    // Способ 3: Через кортежи (tuple destructuring)
    (a, b) = (b, a);  // Самый красивый способ в Rust!
    
    println!("После третьего обмена (способ 3): a = {}, b = {}", a, b);
}
