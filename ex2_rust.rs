/**
 * 🦀 ЗАДАЧА 2 НА RUST: Изменение через мутабельную ссылку
 */

fn main() {
    println!("=== ЗАДАЧА 2: Изменение через ссылку (Rust) ===");
    
    let mut price = 99.99;  // mut = можно изменять
    
    println!("Первоначальная цена: {}", price);
    
    // TODO: Создай мутабельную ссылку и увеличь цену на 10.01
    {
        let ref_price = &mut price;
        *ref_price += 10.01;
        println!("Цена через ссылку: {}", *ref_price);
    } // ссылка умирает здесь
    
    println!("Новая цена: {}", price);
    
    // ВАЖНО: В Rust нельзя одновременно иметь price и ref_price!
    // println!("Цена: {}, Ссылка: {}", price, *ref_price); // ❌ Ошибка!
    
    // Теперь можно использовать price, потому что ссылка уже "умерла"
    println!("Финальная цена: {}", price);  // ✅ Теперь можно!
}
